/**
 * Schema 常量（向后兼容重导出）
 *
 * 实际定义已迁移至 Core 层，此处保留重导出以兼容现有导入路径。
 */
export {
    SCHEMA_METADATA,
    SCHEMA_RESOLUTION,
    SCHEMA_CACHE,
    VERSION_CONDITION,
} from '../../../core/constants/SchemaConstants';
